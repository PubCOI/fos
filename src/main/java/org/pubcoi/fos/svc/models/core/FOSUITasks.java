package org.pubcoi.fos.svc.models.core;

/**
 * A bit rubbish but will do for now ... these are the task types that are sent from the UI (there's a corresponding
 * enum in the UI code)
 *
 */
public enum FOSUITasks {
    mark_canonical_clientNode,
    link_clientNode_to_parentClientNode
}
